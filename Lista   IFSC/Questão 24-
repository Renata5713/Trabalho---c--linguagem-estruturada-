#include <stdio.h>

int main() {

    float velocidade_kmh, velocidade_ms;

    printf("Digite a velocidade em km/h: ");

    scanf("%f", &velocidade_kmh);

    velocidade_ms = velocidade_kmh / 3.6; // 1 km/h = 1/3.6 m/s

    printf("Velocidade em m/s: %.2f\n", velocidade_ms);

    return 0;

}

